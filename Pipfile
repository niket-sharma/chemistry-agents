[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Core CPU-compatible dependencies only
torch = ">=1.9.0"
numpy = ">=1.21.0"
pandas = ">=1.3.0"
scikit-learn = ">=1.0.0"
transformers = ">=4.20.0"
tqdm = ">=4.64.0"
pyyaml = ">=6.0"
requests = ">=2.28.0"
rdkit-pypi = ">=2022.3.0"

# Testing
pytest = ">=7.0.0"
pytest-cov = ">=3.0.0"

[dev-packages]

[requires]
python_version = "3.8"

[scripts]
test = "python tests/test_runner.py"
test-quick = "python tests/test_runner.py --quick"
install-local = "pip install -e ."